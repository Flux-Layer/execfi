name: Build and Deploy to VPS

on:
  push:
    branches:
      - staging
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64
          build-args: |
            NEXT_PUBLIC_APP_NAME=${{ secrets.NEXT_PUBLIC_APP_NAME }}
            NEXT_PUBLIC_PROJECT_ID=${{ secrets.NEXT_PUBLIC_PROJECT_ID }}
            NEXT_PUBLIC_COIN_GECKO_API_KEY=${{ secrets.NEXT_PUBLIC_COIN_GECKO_API_KEY }}
            NEXT_PUBLIC_PRIVY_APP_ID=${{ secrets.NEXT_PUBLIC_PRIVY_APP_ID }}
            NEXT_PUBLIC_PRIVY_APP_SECRET=${{ secrets.NEXT_PUBLIC_PRIVY_APP_SECRET }}
            NEXT_PUBLIC_ZERO_DEV_PROJECT_ID=${{ secrets.NEXT_PUBLIC_ZERO_DEV_PROJECT_ID }}
            NEXT_PUBLIC_ZERO_DEV_PASSKEY_SERVER_URL=${{ secrets.NEXT_PUBLIC_ZERO_DEV_PASSKEY_SERVER_URL }}
            NEXT_PUBLIC_BUNDLER_RPC=${{ secrets.NEXT_PUBLIC_BUNDLER_RPC }}
            NEXT_PUBLIC_BICONOMY_BUNDLER=${{ secrets.NEXT_PUBLIC_BICONOMY_BUNDLER }}
            NEXT_PUBLIC_BICONOMY_PAYMASTER=${{ secrets.NEXT_PUBLIC_BICONOMY_PAYMASTER }}
            NEXT_PUBLIC_BICONOMY_PAYMASTER_API_KEY=${{ secrets.NEXT_PUBLIC_BICONOMY_PAYMASTER_API_KEY }}
            NEXT_PUBLIC_BICONOMY_API_KEY=${{ secrets.NEXT_PUBLIC_BICONOMY_API_KEY }}
            NEXT_PUBLIC_BICONOMY_PROJECT_ID=${{ secrets.NEXT_PUBLIC_BICONOMY_PROJECT_ID }}
            NEXT_PUBLIC_LIFI_KEY=${{ secrets.NEXT_PUBLIC_LIFI_KEY }}
            NEXT_PUBLIC_ALCHEMY_KEY=${{ secrets.NEXT_PUBLIC_ALCHEMY_KEY }}
            NEXT_PUBLIC_DEFAULT_CHAIN_ID=${{ secrets.NEXT_PUBLIC_DEFAULT_CHAIN_ID }}
            OPENROUTER_API_KEY=${{ secrets.OPENROUTER_API_KEY }}
            NEXT_PUBLIC_ENABLE_LIFI_EXECUTION=${{ secrets.NEXT_PUBLIC_ENABLE_LIFI_EXECUTION }}
            MAX_TX_AMOUNT_ETH=${{ secrets.MAX_TX_AMOUNT_ETH }}
            DAILY_SPEND_LIMIT_ETH=${{ secrets.DAILY_SPEND_LIMIT_ETH }}
            NEXT_PUBLIC_PRIVY_SIGNER_ID=${{ secrets.NEXT_PUBLIC_PRIVY_SIGNER_ID }}
            NEXT_PUBLIC_PRIVY_NATIVE_TRANSFER_POLICY_ID=${{ secrets.NEXT_PUBLIC_PRIVY_NATIVE_TRANSFER_POLICY_ID }}
            GAS_HEADROOM_MULT=${{ secrets.GAS_HEADROOM_MULT }}
            MIN_BALANCE_AFTER_TX_ETH=${{ secrets.MIN_BALANCE_AFTER_TX_ETH }}
            CONFIRM_BEFORE_SEND=${{ secrets.CONFIRM_BEFORE_SEND }}
            ENABLE_LIFI_PROVIDER=${{ secrets.ENABLE_LIFI_PROVIDER }}
            ENABLE_RELAY_PROVIDER=${{ secrets.ENABLE_RELAY_PROVIDER }}
            ENABLE_LOCAL_PROVIDER=${{ secrets.ENABLE_LOCAL_PROVIDER }}
            ENABLE_COINGECKO_PROVIDER=${{ secrets.ENABLE_COINGECKO_PROVIDER }}
            NEXT_PUBLIC_LIFI_API_KEY=${{ secrets.NEXT_PUBLIC_LIFI_API_KEY }}
            LIFI_API_KEY=${{ secrets.LIFI_API_KEY }}
            COINGECKO_API_KEY=${{ secrets.COINGECKO_API_KEY }}
            LIFI_PROVIDER_PRIORITY=${{ secrets.LIFI_PROVIDER_PRIORITY }}
            RELAY_PROVIDER_PRIORITY=${{ secrets.RELAY_PROVIDER_PRIORITY }}
            LOCAL_PROVIDER_PRIORITY=${{ secrets.LOCAL_PROVIDER_PRIORITY }}
            COINGECKO_PROVIDER_PRIORITY=${{ secrets.COINGECKO_PROVIDER_PRIORITY }}
            TOKEN_SEARCH_TIMEOUT_MS=${{ secrets.TOKEN_SEARCH_TIMEOUT_MS }}
            PROVIDER_HEALTH_CHECK_INTERVAL_MS=${{ secrets.PROVIDER_HEALTH_CHECK_INTERVAL_MS }}
            TOKEN_CACHE_TTL_SECONDS=${{ secrets.TOKEN_CACHE_TTL_SECONDS }}
            INTEGRATOR_LIFI=${{ secrets.INTEGRATOR_LIFI }}
            NEXT_PUBLIC_LISK_MAINNET=${{ secrets.NEXT_PUBLIC_LISK_MAINNET }}
            NEXT_PUBLIC_LISK_TESTNET=${{ secrets.NEXT_PUBLIC_LISK_TESTNET }}
            NEXT_PUBLIC_ENABLE_ENTRYPOINT=${{ secrets.NEXT_PUBLIC_ENABLE_ENTRYPOINT }}
            NEXT_PUBLIC_ETHERSCAN_API_KEY=${{ secrets.NEXT_PUBLIC_ETHERSCAN_API_KEY }}
            NEXT_PUBLIC_BASE_ACCOUNT_APP_NAME=${{ secrets.NEXT_PUBLIC_BASE_ACCOUNT_APP_NAME }}
            NEXT_PUBLIC_BASE_ACCOUNT_LOGO_URL=${{ secrets.NEXT_PUBLIC_BASE_ACCOUNT_LOGO_URL }}
            NEXT_PUBLIC_BASE_ACCOUNT_SUPPORTED_CHAINS=${{ secrets.NEXT_PUBLIC_BASE_ACCOUNT_SUPPORTED_CHAINS }}
            NEXT_PUBLIC_ENABLE_BASE_ACCOUNT=${{ secrets.NEXT_PUBLIC_ENABLE_BASE_ACCOUNT }}
            NEXT_PUBLIC_BASE_ACCOUNT_AUTO_CREATE=${{ secrets.NEXT_PUBLIC_BASE_ACCOUNT_AUTO_CREATE }}
            NEXT_PUBLIC_BASE_SUB_ACCOUNTS_ENABLED=${{ secrets.NEXT_PUBLIC_BASE_SUB_ACCOUNTS_ENABLED }}
            NEXT_PUBLIC_BASE_SUB_ACCOUNTS_CREATION=${{ secrets.NEXT_PUBLIC_BASE_SUB_ACCOUNTS_CREATION }}
            NEXT_PUBLIC_BASE_SUB_ACCOUNTS_DEFAULT=${{ secrets.NEXT_PUBLIC_BASE_SUB_ACCOUNTS_DEFAULT }}
            NEXT_PUBLIC_BASE_SUB_ACCOUNTS_FUNDING=${{ secrets.NEXT_PUBLIC_BASE_SUB_ACCOUNTS_FUNDING }}
            NEXT_PUBLIC_ENABLE_PAYMASTER=${{ secrets.NEXT_PUBLIC_ENABLE_PAYMASTER }}
            NEXT_PUBLIC_PAYMASTER_PROXY_URL=${{ secrets.NEXT_PUBLIC_PAYMASTER_PROXY_URL }}
            DATABASE_URL=${{ secrets.DATABASE_URL }}
            SESSION_TTL_HOURS=${{ secrets.SESSION_TTL_HOURS }}
            USER_SESSION_MAX_COUNT=${{ secrets.USER_SESSION_MAX_COUNT }}
            CRON_SECRET=${{ secrets.CRON_SECRET }}
            NEXT_PUBLIC_DEGENSHOOT_ADDRESS=${{ secrets.NEXT_PUBLIC_DEGENSHOOT_ADDRESS }}
            NEXT_PUBLIC_WAGER_VAULT_ADDRESS=${{ secrets.NEXT_PUBLIC_WAGER_VAULT_ADDRESS }}
            NEXT_PUBLIC_XP_REGISTRY_PROXY=${{ secrets.NEXT_PUBLIC_XP_REGISTRY_PROXY }}
            NEXT_PUBLIC_DEGENSHOOT_CHAIN_ID=${{ secrets.NEXT_PUBLIC_DEGENSHOOT_CHAIN_ID }}
            NEXT_PUBLIC_DEGENSHOOT_GAME_ID=${{ secrets.NEXT_PUBLIC_DEGENSHOOT_GAME_ID }}
            GAME_SIGNER_PRIVATE_KEY=${{ secrets.GAME_SIGNER_PRIVATE_KEY }}
            ONBOARDING_ENABLED=${{ secrets.ONBOARDING_ENABLED }}
            ONBOARDING_SKIP_ALLOWED=${{ secrets.ONBOARDING_SKIP_ALLOWED }}
            ONBOARDING_DEVICE_TTL_DAYS=${{ secrets.ONBOARDING_DEVICE_TTL_DAYS }}
            PONDER_DATABASE_URL=${{ secrets.PONDER_DATABASE_URL }}
            BASE_SEPOLIA_RPC_URL=${{ secrets.BASE_SEPOLIA_RPC_URL }}
            SUNDAY_QUEST_GAME_ID=${{ secrets.SUNDAY_QUEST_GAME_ID }}
            QUEST_SIGNER_PRIVATE_KEY=${{ secrets.QUEST_SIGNER_PRIVATE_KEY }}
            ADMIN_PRIVATE_KEY=${{ secrets.ADMIN_PRIVATE_KEY }}
            COINFLIP_GAME_ID=${{ secrets.COINFLIP_GAME_ID }}  
            COINFLIP_SERVER_SEED=${{ secrets.COINFLIP_SERVER_SEED }}  
            COINFLIP_SERVER_SEED_EPOCH=${{ secrets.COINFLIP_SERVER_SEED_EPOCH }} 
            COINFLIP_SIGNER_PRIVATE_KEY=${{ secrets.COINFLIP_SIGNER_PRIVATE_KEY }}
            NEXT_PUBLIC_COINFLIP_ADDRESS=${{ secrets.NEXT_PUBLIC_COINFLIP_ADDRESS }}
            NEXT_PUBLIC_COINFLIP_CHAIN_ID=${{ secrets.NEXT_PUBLIC_COINFLIP_CHAIN_ID }}
            NEXT_PUBLIC_COINFLIP_GAME_ID=${{ secrets.NEXT_PUBLIC_COINFLIP_GAME_ID }}
            NEXT_PUBLIC_COINFLIP_VAULT_ADDRESS=${{ secrets.NEXT_PUBLIC_COINFLIP_VAULT_ADDRESS }}
            NEXT_PUBLIC_SHOW_BOMBS=${{ secrets.NEXT_PUBLIC_SHOW_BOMBS }}
            XP_REGISTRY_ADMIN_PRIVATE_KEY=${{ secrets.XP_REGISTRY_ADMIN_PRIVATE_KEY }}

      - name: Deployment complete
        run: |
          echo "✅ Image pushed to ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          echo "🔄 Watchtower will automatically deploy to VPS within 5 minutes"
